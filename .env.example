# Environment Configuration
NODE_ENV=development

# Server Configuration
PORT=8080
PING_MESSAGE=Fusion Invoicing API is running

# Database Configuration (Optional - leave empty to use mock data)
DATABASE_URL=postgresql://username:password@localhost:5432/fusion_invoicing

# Production Settings (for Render.com deployment)
# NODE_ENV=production
# PORT=10000
